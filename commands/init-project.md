---
description: Initialise a new project with Makefile, CI, and .claude/ structure
disable-model-invocation: true
allowed-tools: Read, Write, Bash(mkdir *), Bash(touch *), Bash(cp *)
---

@${CLAUDE_PLUGIN_ROOT}/skills/init-project/SKILL.md
