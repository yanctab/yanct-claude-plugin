---
description: Research a task idea and append it as a detailed task entry to TASKS.md so /execute can pick it up. Use after /tasks to add new tasks incrementally before each /execute run.
disable-model-invocation: true
allowed-tools: Read, Glob, Grep, Write, Agent
---

@${CLAUDE_PLUGIN_ROOT}/skills/new-task/SKILL.md
